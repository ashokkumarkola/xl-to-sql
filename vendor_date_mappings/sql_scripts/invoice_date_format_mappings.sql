UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '486 A Duie Pyle Incorporated (US)'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'AC Facilities BVBA'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Affordable Trucks & Equipment Inc'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Adecco Personnel Services Nv'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Aerocom'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Altebra Fire Protection'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Amarante SRL'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Antargaz NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Ashcco Depot'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Antwerp Towage NV'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Aurelium'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Aqualex'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Associated British Ports'
  AND f.format_code ILIKE 'DD-Mon-YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'AXA Belgium'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'BCR Trade Sarl'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '444 BCTN MEERHOUT NV (BE) (Belgium )'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'BDO Stoy Hayward'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Bechtle Direct LTD'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Bechtle N.V.'
  AND f.format_code ILIKE 'DD-Mon-YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Belfast Container Terminal/BCT'
  AND f.format_code ILIKE 'DD-MON-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Belfast Containers Ni 2000 LTD'
  AND f.format_code ILIKE 'DD/MM?YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Belfruco NV'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Belgische Scheepvaartver Vzw'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Blue Sky Intermodal (Uk) LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Boma NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Brabo Cvba'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Breanheath Limited'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Brim NV - Group Bruno'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'British Telecom-Ee'
  AND f.format_code ILIKE 'DD Mon YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Bupa'
  AND f.format_code ILIKE 'DD Month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'CAI International Inc'
  AND f.format_code ILIKE 'YYYY-MM-DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Canon U.S.A. Inc'
  AND f.format_code ILIKE 'Month DD, YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Cape Fear Intermodal LLC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Central Container Services LTD'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Cape Fear Bonded Warehouse Inc'
  AND f.format_code ILIKE 'MM.DD.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Container Maintenance Corp'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'CMS Transportation Inc.'
  AND f.format_code ILIKE 'MM/DD/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Complete Container Logistics'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Container-Service Friedrich Tiemann & Sohn'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Contargo Weil Am Rhein GmbH'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Contargo Rhein Neckar GMBH (DE)'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '577 Contargo Rhein-Main GmbH - Terminal Koblenz (DE)'
  AND f.format_code ILIKE 'DD.MM.YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '868 Contargo Rhein-Main GmbH - Terminal Gustavsburg (DE)'
  AND f.format_code ILIKE 'DD.MM.YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '868 Contargo Rhein-Main GmbH - Terminal Gustavsburg (DE)'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Contargo Woerth GmbH'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Crossroad Communications NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Customs Made Clearance NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Dakosy Datenkommunikationssystem AG'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Danny Govaerts'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Danser Container Line BV'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Dartswift International LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Dartswift International Ltd - Duty & VAT'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'De Fruitkorf'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Delaware River Harbor Pilots LLC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Denholm Port Services'
  AND f.format_code ILIKE 'DD.MON.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Descartes Systems Nl BV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Diamond Shipping Limited'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '470 DIT Duisburg Intermodal Terminal GMBH (DE)'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'DKV Belgium'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Doc N Loc'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Doyle Shipping Group'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Dustin Belgie NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Dutch Container Merchants B.V.'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Eco Worldwide Solutions N.V.'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'EDENRED BELGIUM SA/NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Eldapoint LTD Wbp'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Electrabel NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Ellerman City Liners Limited'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Eucon Shipping & Transport LTD'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'EUCORE/European Combined Reefers BVBA'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Eurodist NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'ECA Antwerp nv'
  AND f.format_code ILIKE 'DD MM YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Exxonmobil Marine LTD'
  AND f.format_code ILIKE 'DD MMM YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Federale Politie'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Fideliti Limited'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Flagship Logistics LTD.'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Florens Asset Management (Usa) LTD'
  AND f.format_code ILIKE 'DD-Mon-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'FOD Financien'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Fraser Haulage LTD Wbp'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Freightliner LTD Wbp'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Fruit At Work'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'GAC North America'
  AND f.format_code ILIKE 'DD-Month-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Gielen Containers'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Glazenwasserij Javaca Vof'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Gosselin Logistics NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'GS EVENTS LTD'
  AND f.format_code ILIKE 'DD/jan/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Haeger & Schmidt Logistics GmbH'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Haulage Express LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Hapag-Lloyd'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Haven van Antwerpen Brugge nv'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Hedin Automotive Kontich'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Herfurth Logistics'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Hestia NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '466 HGK Intermodal Koeln GmbH'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Highgate Solutions'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Hutchison Ports Venl'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Inchcape Shipping Services NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Inchcape Shipping Services UK Ltd'
  AND f.format_code ILIKE 'DD Month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Interligo NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Irish Shipping And Transport LTD.'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Jeffrey-Fenneman Law And Strategy Pllc'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'John Hurst - Milkman'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'John R Savoy Ent Inc'
  AND f.format_code ILIKE 'MON DD, YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Johnson Stevens (Belfast NI) LTD GBP'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Johnson Stevens (NI) Ltd. EUR'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Johnson Stevens (Dublin) LTD EUR'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'KBC Autolease NV'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'KBC Bank NV'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'KBC Bank NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Koffiemax België B.V.'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Koffie/Cafes F. Rombouts NV'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Konica Minolta Business Solutions'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'KRAMER CONTAINERS B.V.'
  AND f.format_code ILIKE 'DD-Mon-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Kubrechts BVBA'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Langtons Payroll Services Limited'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Lankel Marine Container Refrigeration'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Leep Electricity Services LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Ligna NV'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'liquidline Ltd.'
  AND f.format_code ILIKE 'XX Month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Locallnet LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Loodswezen B.V.'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Luminus'
  AND f.format_code ILIKE 'MM-DD-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Lyneer Staffing Solutions, LLC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Lyreco Belgium NV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Manutan NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Maritime Cargo Processing'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Masterfreight'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Mcallister Towing'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Medicash Health Benefits LTD'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'MEN T GROUP NV'
  AND f.format_code ILIKE 'DD Month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Mercure Hotel'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Metlife'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Metlife Small Business'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Metro Shipping Ltd'
  AND f.format_code ILIKE 'DD-Mon-YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Meeus - Brood En Banket'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Meyer Antwerp BVBA'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'MHS Lift Inc'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Mid-Atlantic Propane Exchange LLC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Mobitel'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Motrac NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Mtd Maritime Services BVBA'
  AND f.format_code ILIKE 'DD.month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'NACPC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'New Power Soul Club'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'NC State Ports Authority'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Northern Containers LTD'
  AND f.format_code ILIKE 'YYY-MM-DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Nurture Landscapes LTD'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'NxtPort BV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Nys Machinery Equipment'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'O2 TELEPHONE'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Octa+ Energie N.V.'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'OK Slurink'
  AND f.format_code ILIKE 'DD month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'On Time Sprint BV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '1070 Optimodal Nederland BV (NL)'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Orbcomm Europe BV'
  AND f.format_code ILIKE 'YYYY-MM-DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Penn Terminals Inc'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Port Of Cork Company'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Peel Property (Investments) LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Peel Water Services Limited'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'peter doehle schiffahrts-kg daily futures'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Plantrent Office BV'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Pla Soft Drinks LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Port Of Limburg NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Portico Shipping Limited'
  AND f.format_code ILIKE 'DD-Mon-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Portilog'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Prestige Recruitment'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Pro-Log BV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'PROXIMUS DIRECT DEBIT'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Proximus'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Radius Business Solutions (Belgium) NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Russell Railroad LTD'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Sapphire House Antwerp'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'S.D.M.-Sociedade Desenvolvimento Da Madeira, S.A'
  AND f.format_code ILIKE 'YYYY-MM-DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'S&R Packing & Crating LLC'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'SD Worx VZW'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'SD WORX SOCIAAL SECRETARIAAT VZW'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Seacube Container Leasing'
  AND f.format_code ILIKE 'YYYY-MM-DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Sertalink'
  AND f.format_code ILIKE 'MM DD, YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Servipark NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Shipowners Protection Fund'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Socon Services'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Sodexo Belgium NV'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Solent Stevedores Limited'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'John A. Steer'
  AND f.format_code ILIKE 'DD-Mon-YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Stormgeo Corp Inc'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Svitzer'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Synigent Technologies Inc'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Telenet Solutions Division'
  AND f.format_code ILIKE 'DD Month YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'The Pilots'' Association For'
  AND f.format_code ILIKE 'MM/DD/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Thornton Spares'
  AND f.format_code ILIKE 'MM/DD/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Tiscotex'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Tradeshift Belgium SA- babelway'
  AND f.format_code ILIKE 'DD MON YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Transporeon GmbH'
  AND f.format_code ILIKE 'YYYY.MM.DD'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Tri-State Refrigerated Inc'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'UES International Leasing Limited'
  AND f.format_code ILIKE 'DD-Mon-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Unifirst Corporation'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Valor Services'
  AND f.format_code ILIKE 'DD-MM-YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Vincotte nv'
  AND f.format_code ILIKE 'DD.MM.YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Vlaamse Overheid Amdk'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Waltbrechts BVBA'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Water-Link'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Wilmington Cape Fear Pilots'
  AND f.format_code ILIKE 'MM/DD/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Wilmington Docking Pilots'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Wilmington Shipping Company'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE '1446 Williams Shipping Group (GB)'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'Antwerp Container Terminal NV'
  AND f.format_code ILIKE 'DD/MM/YY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'DP World Southampton'
  AND f.format_code ILIKE 'DD/MM/YYY'
  AND m.is_deleted = FALSE;

UPDATE master_lido_mapping m
SET invoice_date_format_id = f.id
FROM vendors v, invoice_date_formats f
WHERE m.vendor_id = v.id
  AND v.vendor_name ILIKE 'SSA Atlantic LLC'
  AND f.format_code ILIKE 'DD/MM/YYYY'
  AND m.is_deleted = FALSE;

